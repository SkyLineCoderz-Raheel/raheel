import 'package:flutter/material.dart';

class MediumText extends StatelessWidget {
  const MediumText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
