import 'package:flutter/material.dart';

class LargeText extends StatelessWidget {
  const LargeText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
